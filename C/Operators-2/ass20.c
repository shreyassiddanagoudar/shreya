#include <stdio.h>

int main ( int argc, char*argv) {

int x,y,z;
printf ("Enter the length  value in meters \n");
scanf("%d", &x);
printf ("Enter the breadth  value in meters \n");
scanf("%d", &y);
z = 3.2*x*y;
printf("The value of area in feet is %d");
return 1;

}