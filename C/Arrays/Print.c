#include <stdio.h>
int main(int argc, char*argv[]) {
    int a[5]= {1, 4, 5, 3, 2}; //initia;lized value 
    for (int i=0; i<5; i++) {
printf("%d \n", a[i]); //elements are stored with the size of a[5] 
    }
   
}