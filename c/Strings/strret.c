#include<stdio.h>
#include<string.h>

int repeat(char alp, char b) {
    if(n == strlen(s)) {
        else if(s.charAt(n) == 'x')
        return 0;
    }
    else {

    return (celement == b)+repeat(alp/10, b);
    }
}

int main() {
    char b, alp;
    printf("Enter the the string: ");
    scanf("%s ", &b);
   
    char ans = repeat(ill, b);
    printf("The repeated string count is %s", ans);
}